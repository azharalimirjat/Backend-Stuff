// const math = require('./math');

const {add, subFn} = require('./math')

// console.log('The sum is:', math.addFn(2,4));

// console.log('The sub is:', math.subFn(8,4));

console.log('The sub is:',subFn(8,4));